name: Deploy candidate vision from main

on:
  issue_comment:
    types: [created]

permissions:
  contents: read
  pages: write
  id-token: write
  pull-requests: write
  issues: write

concurrency:
  group: pages-main
  cancel-in-progress: false

jobs:
  deploy:
    if: github.event.issue.pull_request && contains(github.event.comment.body, 'deploy-pages')
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install verification dependencies
        run: python -m pip install --disable-pip-version-check pypdf
      - name: Verify campaign
        run: python scripts/verify_campaign.py
      - uses: actions/configure-pages@v5
        with:
          enablement: true
      - uses: actions/upload-pages-artifact@v3
        with:
          path: .
      - id: deployment
        uses: actions/deploy-pages@v4
      - name: Report deployment URL
        if: always()
        env:
          GH_TOKEN: ${{ github.token }}
          PAGE_URL: ${{ steps.deployment.outputs.page_url }}
        run: |
          if [ -n "$PAGE_URL" ]; then
            gh pr comment "${{ github.event.issue.number }}" --body "PAGES_DEPLOYMENT_URL=$PAGE_URL"
          else
            gh pr comment "${{ github.event.issue.number }}" --body "PAGES_DEPLOYMENT_FAILED_AFTER_VERIFICATION"
          fi
